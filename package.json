{
  "dependencies": {
    "cypress": "^12.7.0"
  },
  "devDependencies": {
    "@cypress/xpath": "^2.0.3"
  },
  "scripts": {
    "gui": "npx cypress open",
    "headlessChrome": "cypress run --headless --browser chrome"
  }
}

